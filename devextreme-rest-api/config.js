const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "root",
    password: "Baci8899$",
    database: "testapi",
  },
  listPerPage: 100,
};

module.exports = config;
